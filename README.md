## Template test assignment to use jupyter sandbox in codio

In guides layout page settings set your jupyter sandbox in open tabs and save.
Run assignment and use files from workspace by auto-sync to sandbox. Execute scripts in sandbox to check requests to LLM (in LLM enabled courses) and check GPU instance.